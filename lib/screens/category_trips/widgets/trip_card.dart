part of '../widgets_barrel.dart';

class _TripCard extends StatelessWidget {
  const _TripCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
